# MCP config — contains the YouTube Data API key
.mcp.json

# deps & build
node_modules/
dist/
build/
dist-mcpb/
*.mcpb
*.tgz

# embeddings index & local data
*.sqlite
*.sqlite-journal
data/

# local dev only — personal Claude Code config/skills and packaging helpers,
# not part of the public repo
.claude/
Makefile

# editor / OS
.DS_Store
.idea/*
!.idea/.gitignore
!.idea/dictionaries
!.idea/dictionaries/*

# logs
*.log

#extra dir
extra/*

# local research/idea prompts — reference gitignored extra/ zips, kept local
docs/prompts/

# internal design/research/decision docs — kept local, not part of the public repo
docs/dev/

# retrieval-eval work dir (model cache copy + throwaway fixture index)
test/eval/retrieval/.work/

# local mockups, recordings, handoff zips — demo.gif lives at repo root
assets/

# stray dir from a mis-expanded "~/Library/Application Support/Claude" path
Support/
